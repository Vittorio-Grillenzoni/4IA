#include <stdio.h> //gcc -std=c99 file.c && ./a.out
/**stampa la matrice argomento */
void printMatrix(int rows, int cols, int m[rows][cols])



#define DIM 3{
    int m[DIM][DIM] = {
        {0, 1, 2},
        {10, 11, 12},
        {20, 21, 22},
    };

    //printfMatrix(DIM, DIM, m);
    return 0;
}